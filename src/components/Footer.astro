---
const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-content">
    <div class="footer-decoration" aria-hidden="true">
      <span class="deco-item">✨</span>
      <span class="deco-item">📚</span>
      <span class="deco-item">🎯</span>
      <span class="deco-item">🚀</span>
      <span class="deco-item">💡</span>
    </div>
    <p class="footer-text">
      Made with <span class="heart">❤️</span> using <strong>Astro</strong>
    </p>
    <p class="footer-copy">&copy; {currentYear} Learning Hub</p>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 6rem;
    padding: 2.5rem 0 2rem;
    border-top: 2px solid var(--color-border);
    background: var(--color-bg);
    position: relative;
  }

  .footer-content {
    text-align: center;
    color: var(--color-text-muted);
  }

  .footer-decoration {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }

  .deco-item {
    display: inline-block;
  }

  .deco-item:nth-child(1) { animation-delay: 0s; }
  .deco-item:nth-child(2) { animation-delay: 0.5s; }
  .deco-item:nth-child(3) { animation-delay: 1s; }
  .deco-item:nth-child(4) { animation-delay: 1.5s; }
  .deco-item:nth-child(5) { animation-delay: 2s; }

  .footer-text {
    font-size: 0.9375rem;
    margin: 0 0 0.5rem;
  }

  .heart {
    color: var(--color-secondary);
  }

  .footer-text strong {
    color: var(--color-primary);
    font-weight: 700;
  }

  .footer-copy {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    margin: 0;
  }
</style>
