---
interface Props {
  difficulty: 'beginner' | 'intermediate' | 'advanced';
  size?: 'sm' | 'md';
}

const { difficulty, size = 'md' } = Astro.props;

const icons = {
  beginner: '🌱',
  intermediate: '⚡',
  advanced: '🔥'
};
---

<span class={`difficulty-badge ${difficulty} ${size}`}>
  <span class="badge-icon">{icons[difficulty]}</span>
  <span class="badge-text">{difficulty}</span>
</span>

<style>
  .difficulty-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.375rem 0.875rem;
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border: 2px solid;
  }

  .difficulty-badge.sm {
    padding: 0.25rem 0.625rem;
    font-size: 0.75rem;
    gap: 0.25rem;
  }

  .badge-icon {
    font-size: 1em;
    line-height: 1;
  }

  .difficulty-badge.beginner {
    background: var(--color-beginner-bg);
    color: var(--color-beginner);
    border-color: var(--color-beginner);
  }

  .difficulty-badge.intermediate {
    background: var(--color-intermediate-bg);
    color: var(--color-intermediate);
    border-color: var(--color-intermediate);
  }

  .difficulty-badge.advanced {
    background: var(--color-advanced-bg);
    color: var(--color-advanced);
    border-color: var(--color-advanced);
  }
</style>
